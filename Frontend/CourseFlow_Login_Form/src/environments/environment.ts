/* Environment variables for Application
change firebase config variables if the owner is changed */

export const environment = {
  production: false,

  /* Firebase configuration: User can use firebase config keys
  from the provided CouseFlow Firebase account here.

  firebaseConfig: {
  }
  */
 
};

